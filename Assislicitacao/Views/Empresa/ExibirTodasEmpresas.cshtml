@model IEnumerable<Empresa>

@{
    ViewData["Title"] = "Exibir Empresas";

    if (TempData["FalhaVincular"] != null) {
        <div class="alert alert-danger" role="alert">
            @TempData["FalhaVincular"]
        </div>
    } else if (TempData["SucessoVincular"] != null) {
        <div class="alert alert-success" role="alert">
            @TempData["SucessoVincular"]
        </div>
    }

    var tipoUsuario = Context.Session.GetString("usuarioTipoUsuario");
}
<table class="table">
    <thead>
        <tr>
            <th scope="col">RAZÃO SOCIAL</th>
            <th scope="col">CNPJ</th>
            <th scope="col">PORTE</th>
            @if (tipoUsuario == "REPRESENTANTE LEGAL" || tipoUsuario == "ADMINISTRADOR DE SISTEMA") {
                <th scope="col">USUARIOS VINCULADOS</th>
            }
            <th scope="col">AÇÕES</th>
        </tr>
    </thead>
    <tbody>
        @foreach (Empresa Empresa in Model) {
            <tr>
                <td>@Empresa.RazaoSocial</td>
                <td>@Empresa.CNPJ</td>
                <td>@Empresa.PorteEmpresa.Porte</td>
                @if (tipoUsuario == "REPRESENTANTE LEGAL" || tipoUsuario == "ADMINISTRADOR DE SISTEMA") {
                    <td>@Empresa.UsusariosVinculados.Count</td>
                }
                <td>
                    <a asp-controller="Empresa" asp-action="ExibirEmpresa" asp-route-id="@Empresa.Id">Exibir</a>
                    @if (tipoUsuario == "ADMINISTRADOR DE SISTEMA") {
                        <span>|</span>
                        <a asp-controller="Empresa" asp-action="ConfirmarExclusaoEmpresa" asp-route-id="@Empresa.Id">Excluir</a>
                    }
                    @if (tipoUsuario == "REPRESENTANTE LEGAL" || tipoUsuario == "ADMINISTRADOR DE SISTEMA") {
                        <span>|</span>
                        <a asp-controller="Usuario" asp-action="PesquisarUsuariosPorEmail" asp-route-id="@Empresa.Id">Vincular Usuarios</a>
                        <span>|</span>
                        <a asp-controller="Empresa" asp-action="EnviarRelatorioLicitacoes" asp-route-empresaId="@Empresa.Id">Relatório Licitações (email)</a>
                        <span>|</span>
                        <a asp-controller="Relatorio" asp-action="GerarRelatorioLicitacoes" asp-route-empresaId="@Empresa.Id" target="_blank">Relatório Licitações (PDF)</a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
